DEBUG: true

UART:
  PORT: /dev/cu.usbmodem0E2208FC1
  BAUDRATE: 19200

QUBE:
  SIMULATION: true
  VISUALIZE: true
  MODERN_STABILIZATION: true
  SIMULATION_SPEED: 1  # 1.0 = real time, 0.5 = half speed

CONTROL:
  # Control loop timing
  dt: 0.001                    # Control timestep [s] (1 kHz)
  duration: null               # Maximum simulation duration [s], set to null for unlimited
  
  # Swing-up thresholds
  swingup_alpha_threshold: 0.3         # Pendulum angle threshold for mode switch [rad]
  swingup_alpha_dot_threshold: 2.0     # Pendulum velocity threshold for mode switch [rad/s]
  
  # LQR stabilization gains [theta, theta_dot, alpha, alpha_dot]
  # These are tuned to balance the pendulum at upright and center the arm
  lqr_gains:
    - 1.5      # k_theta: penalize arm displacement from center
    - 0.5      # k_theta_dot: penalize arm velocity
    - 15.0     # k_alpha: penalize pendulum displacement from upright
    - 3.0      # k_alpha_dot: penalize pendulum velocity

PLANT:
  # Physical/simulation parameters
  motor_constant: 0.05         # Motor torque constant [Nm/V]
  arm_length: 0.085            # Distance from pivot to encoder axis [m]
  pendulum_length: 0.129       # Length of pendulum rod [m]
  arm_mass: 0.15               # Mass of arm [kg]
  pendulum_mass: 0.05          # Mass of pendulum [kg]
  gravity: 9.82                # Gravitational acceleration [m/s^2]
  
  # Voltage limits
  voltage_max: 10.0            # Maximum motor voltage [V]
  voltage_min: -10.0           # Minimum motor voltage [V]
